{
  "name": "real-estate-root",
  "private": true,
  "dependencies": {
    "google-maps-react": "^2.0.6"
  },
  "scripts": {
    "postinstall": "npm --prefix real-estate-site install",
    "build": "npm --prefix real-estate-site run build",
    "start": "npm --prefix real-estate-site run start"
  }
}
